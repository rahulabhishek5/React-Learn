import React from 'react'

const header = () => {
  return (
    <div>/* This is module CSS for the Header component.this is the way CSS is implemented in React. We can write CSS in a separate file and import it into the component. This way we can keep our styles organized and modular. We can also use CSS modules to scope our styles to the component, so that they don't affect other components. */</div>
  )
}

export default header